import * as React from 'react';
import { TextEditor } from '../component/TextEditor';

const IndexPage = () => {
  return (
    <div>
      <TextEditor />
    </div>
  );
};

export default IndexPage;
